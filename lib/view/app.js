// Generated by CoffeeScript 1.6.3
(function() {
  define(['R', '_'], function(R, _) {
    return {
      init: function() {
        return $(document).ready(function() {
          var descriptionEnabled;
          $('dd').css('display', 'none');
          descriptionEnabled = false;
          $(".btn-info").click(function() {
            if (descriptionEnabled) {
              $('dd').css('display', 'none');
              $('.container').css('width', '');
              return descriptionEnabled = false;
            } else {
              $('dd').css('display', 'inline').addClass('uk-animation-scale-up');
              $('.container').css('width', '700px');
              return descriptionEnabled = true;
            }
          });
          $('.btn-more-urls').click(function() {
            return chrome.tabs.create({
              'url': "chrome://about/"
            });
          });
          $('.chrome-link').click(function() {
            var $this;
            $this = $(this);
            if ($this.siblings('.uk-icon-external-link').size() > 0) {
              return _.redirectTo($this.text());
            } else {
              $this.attr('id', 'current');
              _.copyToClipboard($this.text());
              _.selectText('current');
              return $this.attr('id', '');
            }
          });
          return $('.badge-warn').attr('data-uk-tooltip', '').attr('title', R.string.warning);
        });
      }
    };
  });

}).call(this);
